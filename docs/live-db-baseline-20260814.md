# خط أساس قاعدة البيانات المنشورة — 14 أغسطس 2026

## المشروع

- مرجع Supabase المطابق لعميل التطبيق: `rosmvxigwcoclamawjps`.
- الحالة: `ACTIVE_HEALTHY`.
- الترحيلات المنشورة متطابقة زمنياً مع ترحيلات المستودع حتى `20260812013507`، ولم تكن ترحيلة المحرك الذري الجديدة منشورة عند أخذ خط الأساس.

## حالة البيانات المالية

| المؤشر | القيمة |
|---|---:|
| رؤوس قيود بلا `company_id` | 2 |
| قيود مرحّلة ذات مجاميع غير متوازنة أو صفرية | 0 |
| بنود قيود يتيمة بلا رأس | 0 |
| بنود بقيَم سالبة أو مدين ودائن معاً | 0 |

لا تُحدّث القيود التاريخية التي بلا شركة تلقائياً؛ يلزم ربطها بعد مراجعة المصدر والكيان الصحيحين.

## نقاط الحماية المكتشفة

- كانت سياسات RLS تسمح بإدراج وتحديث مباشر على `journal_entries` لمن يملك وصولاً مالياً، وتسمح سياسة `jei_all` بكل عمليات DML على بنود القيود.
- كانت دالة `can_access_company_finance` تعتمد على `has_any_role` العام وتضم الدور القديم `finance_manager`، بدلاً من تقييد الدور بالشركة المستهدفة.
- كشف فحص الأمان عن دوال `SECURITY DEFINER` متعددة قابلة للتنفيذ من `anon`، إضافة إلى دوال لها `search_path` قابل للتغيير. تُعالج هذه المجموعة في ترحيلات تقوية لاحقة، بدءاً بدوال الأستاذ والمحرك الجديد.

## البنية الفعلية ذات الصلة

- يدعم `journal_entries` حقول الشركة والحالة والمجاميع والمستخدمين، وكانت `company_id` قابلة للقيمة الفارغة تاريخياً.
- يدعم `journal_entry_items` الحساب والمدين والدائن والأبعاد والضريبة؛ وكان `journal_entry_id` قابلاً للقيمة الفارغة في المخطط.
- يربط `chart_of_accounts` الحساب بالشركة اختيارياً ويضم حالة نشاط الحساب.
- الأدوار مخزنة في enum `app_role`؛ النموذج الحالي للتطبيق هو `admin`, `owner`, `accountant`, `hr_manager`, `sales_manager`, `viewer`.
