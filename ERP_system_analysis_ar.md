# تقرير تحليل نظام ERP وخارطة طريق التكامل

**المستودع محل التحليل:** [`Turkey12st/pro-2`](https://github.com/Turkey12st/pro-2)  
**تاريخ التحليل:** 14 أغسطس 2026  
**نطاق التحليل:** مراجعة ساكنة للكود، مخطط البيانات والترحيلات، منطق المصادقة والصلاحيات، الخدمات المالية، واجهة التطبيق، وفحص البناء محلياً. لم يتضمن التحليل اختباراً على بيانات إنتاج حية أو تعديلاً على قاعدة البيانات المنشورة.

> **الخلاصة التنفيذية:** التطبيق قابل للبناء ويملك واجهة ERP عربية جيدة ونواة مناسبة من React وSupabase، إلا أنه ليس جاهزاً بعد ليكون نظاماً مالياً إنتاجياً مترابطاً. توجد ضوابط جيدة أُضيفت في ترحيلات حديثة لسلامة القيود، لكن خدمات واجهة أقدم ومسارات موازية ما زالت تتجاوز النموذج المحاسبي الصحيح أو تتعارض معه. الأولوية ليست إضافة صفحات جديدة، بل توحيد الهوية والشركة النشطة، وإغلاق مسارات الترحيل المباشر، وتحويل إنشاء القيود إلى محرك خلفي ذريّ وقابل للتدقيق.

## 1. النتيجة العامة

| المحور | الحالة الحالية | التقييم | القرار المطلوب |
|---|---|---:|---|
| البناء الإنتاجي | ينجح `vite build`، لكن الحزمة الرئيسية تبلغ نحو 2.8 MB غير مضغوطة و815 KB مضغوطة. | متوسط | تقسيم المسارات والحزم الثقيلة قبل التوسع. |
| جودة الكود | فحص الجودة يسجل 301 خطأ و43 تحذيراً. | حرج | منع الدمج إلى الفرع الرئيسي حتى تُعالج أخطاء الحجب والأخطاء عالية المخاطر. |
| الاعتمادات | يوجد عدم اتساق بين `package.json` و`package-lock.json`، إضافة إلى تعارض peer dependency بين مكتبات PDF. | حرج | اعتماد ملف قفل واحد صحيح وإصدار حزم متوافق. |
| المصادقة | تسجيل الدخول وحماية المسارات موجودان، لكن استعادة كلمة المرور وحالة الجلسة تحتاجان تحسيناً. | متوسط | توحيد معالج أحداث المصادقة ومعالجة تدفق الاسترداد. |
| RBAC والعزل بين الشركات | النموذج موجود لكنه غير متسق بين الواجهة والدوال وسياسات قاعدة البيانات، وبعض التحقق لا يقيّد الشركة. | حرج | بناء تفويض مركزي قائم على العضوية في الشركة والصلاحية الدقيقة. |
| دفتر الأستاذ العام | توجد بنود قيود وضوابط حديثة للتوازن والحماية بعد الترحيل. | متوسط | اعتمادها كالمسار الوحيد للترحيل وإزالة المسارات المتعارضة. |
| الأتمتة المالية | توجد أكثر من خدمة لترحيل رواتب/معاملات، وبعضها ينفذ من المتصفح وبلا معاملة ذرية أو idempotency. | حرج | نقل جميع أحداث الترحيل إلى وظائف خادمية/RPC واحدة وذرية. |
| المبيعات والمشتريات والمخزون | ليست مكتملة كنموذج دورة مستندية: لا توجد جداول مبيعات/مشتريات/مخزون معيارية أساسية. | حرج | تصميم هذه الدورات قبل الادعاء باكتمال ERP أو الفوترة الإلكترونية. |
| التقارير | خدمة القوائم تعتمد على بنود القيود المرحّلة، وهو اتجاه صحيح، لكن لا تختار الشركة صراحةً وبعض المؤشرات تستخدم UUID كنص حساب. | مرتفع | توحيد مصدر التقارير على دفتر الأستاذ وفلتر الشركة والفترة. |
| تجربة الاستخدام | واجهة تسجيل الدخول العربية سليمة بصرياً؛ التنقل وبيانات المستخدم افتراضية ولا تعكس الدور الفعلي. | مرتفع | ربط الهوية والتنقل بالصلاحيات والحالة الحية للمستخدم. |

## 2. ما يعمل حالياً وما لا يكفي للإنتاج

نجح بناء المشروع محلياً دون أخطاء TypeScript ظاهرة في مرحلة Vite، وهذا يعني أن التطبيق يمكن توزيعه تقنياً. إلا أن النجاح في البناء لا يثبت سلامة قواعد العمل أو سلامة البيانات المالية. فحص الجودة الساكن كشف عدداً كبيراً من المشكلات، كما أن تحذير حجم الحزمة يشير إلى تحميل مكوّنات وميزات كثيرة في ملف JavaScript رئيسي واحد.

يحتوي المشروع على نواة جيدة تشمل صفحة مصادقة عربية، مسارات محمية، صفحات للحسابات والموارد البشرية والمشاريع والعملاء ورأس المال والمطابقة البنكية، إضافة إلى جداول للقيود وبنود القيود ودليل الحسابات. وتوجد ترحيلات حديثة تضيف حماية للقيود المرحّلة وتتحقق من توازنها عند ترحيلها. لكن هذه العناصر لا تعمل حتى الآن كنظام واحد متسق بسبب وجود خدمات قديمة أو متوازية تستخدم هياكل مختلفة أو تتصرف من طرف العميل مباشرةً.

## 3. الأخطاء الحرجة والمرتفعة الأولوية

### 3.1 الاعتمادات وقياس جودة الكود

| المعرف | الخطورة | الدليل | الأثر | الحل الأفضل |
|---|---:|---|---|---|
| B-01 | حرج | `package.json` لا يطابق `package-lock.json`؛ لذلك فشل `npm ci` قبل استخدام تثبيت متسامح محلياً. | لا يمكن ضمان أن البيئة المحلية وبيئة النشر تستعملان الاعتمادات نفسها. | اختيار مدير حزم واحد، إزالة ملفات القفل غير المستخدمة، إعادة توليد lockfile من بيئة نظيفة، ثم فرض `npm ci` في CI. |
| B-02 | مرتفع | تعارض peer dependency بين `jspdf` و`jspdf-autotable`. | قد يتعطل تصدير PDF أو تصبح نتائج التثبيت غير قابلة للتكرار. | اعتماد زوج إصدارات متوافق رسمياً ثم تغطية التصدير باختبار دخاني. |
| B-03 | حرج | فحص ESLint سجّل 301 خطأ و43 تحذيراً، منها `any` واسع، وأخطاء تحليل في دوال Supabase، واستيراد `require`. | الأخطاء الحقيقية تختفي وسط الضوضاء، وتزداد مخاطر التراجع عند التعديل. | معالجة أخطاء التحليل أولاً، ثم تمكين قواعد TypeScript تدريجياً حسب المجلد، ورفض أخطاء lint في CI بعد خط أساس مؤقت. |
| B-04 | مرتفع | `vite build` أصدر تحذيراً عن ملف رئيسي يبلغ 815 KB gzip. | بطء التحميل الأولي، خاصة على شبكات الجوال. | استخدام `React.lazy` للمسارات، وتأجيل المكتبات الثقيلة مثل PDF وXLSX والمخططات إلى الشاشات التي تحتاجها، وتقسيم vendor chunks. |

### 3.2 المصادقة وإدارة الجلسات

| المعرف | الخطورة | الدليل | الأثر | الحل الأفضل |
|---|---:|---|---|---|
| A-01 | مرتفع | `useAuth.ts` يستدعي RPC تهيئة المستخدم بعد الدخول بمهلة صفرية ولا ينتظر نجاحها قبل إتاحة التطبيق. | قد يدخل المستخدم لوحة التحكم قبل إنشاء العضوية أو الدور، فتظهر صلاحيات افتراضية أو حالات تحميل غير مستقرة. | تنفيذ bootstrap خادمياً عند إنشاء المستخدم، أو انتظار RPC صريحة قبل اكتمال حالة الجلسة؛ ويجب إظهار حالة فشل قابلة للفهم. |
| A-02 | مرتفع | `ResetPassword.tsx` يتحقق فقط من `getSession()` عند التحميل. | روابط الاسترداد التي تتطلب تبادل رمز أو تنتج حدث استعادة متأخراً قد تُصنف كرابط غير صالح. | الاستماع إلى `PASSWORD_RECOVERY` و`SIGNED_IN`، ودعم تبادل رمز الاسترداد عند وجوده، ثم منع تحديث كلمة المرور قبل تحقق الجلسة. |
| A-03 | متوسط | التحقق من كلمة المرور في الدخول يسمح بستة أحرف، بينما إعادة التعيين تتطلب ثمانية. | سياسة كلمة المرور غير متسقة، ورسالة التجربة مربكة. | تعريف سياسة واحدة في ملف مشترك وتطبيقها على الإنشاء وتسجيل الدخول وإعادة التعيين وإدارة المستخدمين. |
| A-04 | مرتفع | إنشاء المستخدم في `admin-users` يفعّل البريد مباشرة، بينما مسار التسجيل العام يفترض تأكيد البريد. | السلوك الأمني وتوقعات المستخدم تختلف حسب مسار الإنشاء. | فصل تدفق الدعوة الإدارية عن التسجيل العام، واعتماد سياسة تأكيد واحدة لكل نوع مستخدم. |

### 3.3 الصلاحيات وعزل الشركات

| المعرف | الخطورة | الدليل | الأثر | الحل الأفضل |
|---|---:|---|---|---|
| S-01 | حرج | أنواع الأدوار المركزية هي `admin/owner/accountant/hr_manager/sales_manager/viewer`، بينما واجهات الإدارة ودوال الاعتماد تستخدم أدواراً قديمة مثل `finance_manager` و`employee`. | قد يفشل حفظ الدور أو اعتماد القيد، أو تمنح الواجهة خيارات لا تقبلها قاعدة البيانات. | اعتماد enum واحد كمصدر حقيقة، وإجراء migration لتحويل الأدوار القديمة، ثم مشاركة ثوابت الدور بين الواجهة والدوال والسياسات. |
| S-02 | حرج | دالة `has_any_role` تتحقق من المستخدم والدور فقط ولا تأخذ `company_id`، وتستخدمها سياسات و`approve_request`. | مدير أو محاسب في شركة قد يحصل على صلاحية على سجلات شركة أخرى عند وجود عضويات متعددة. | استبدالها بدالة مثل `has_company_permission(user_id, company_id, permission)`، واستخدامها في كل RPC وRLS. |
| S-03 | حرج | دالة `approve_request` تتضمن `finance_manager` غير الموجود في نموذج الدور، ولا تتحقق من عضوية الشركة التي يخصها القيد. | اعتماد القيود قد يتعطل أو يتجاوز فصل الكيانات. | تمرير شركة القيد إلى دالة الصلاحية، واستخدام مصفوفة أدوار صحيحة أو صلاحية `approve_journal_entry` مستقلة. |
| S-04 | مرتفع | `ProtectedRoute` يفرض الدور فقط على الإدارة واللوحة التنفيذية؛ بقية المسارات تتطلب تسجيل دخول فحسب. | المستخدم المصادق عليه يستطيع فتح صفحات لا تناسب وظيفته، حتى لو حالت RLS دون بعض البيانات لاحقاً. | تعريف صلاحية مطلوبة لكل route ولكل action، وإخفاء عناصر التنقل غير المصرح بها مع استمرار حماية الخادم. |
| S-05 | مرتفع | `AppLayout` و`AppNavigation` يعرضان مستخدماً افتراضياً وبريداً افتراضياً، وقائمة التنقل كاملة لكل مستخدم. | لا تعكس الواجهة هوية المستخدم أو الدور أو الشركة النشطة، وتخلق تصوراً خاطئاً للصلاحيات. | بناء `SessionProvider` واحد يحمّل المستخدم والعضوية والشركة النشطة والصلاحيات؛ وتصفية التنقل عبره. |
| S-06 | متوسط | دالة `admin-users` تستعلم عن دور المستخدم بـ`.single()` من دون اختيار الشركة. | يفشل التحقق أو يصبح غير حتمي عندما تكون للمستخدم أدوار في أكثر من شركة. | تمرير `company_id` من سياق الشركة، وإلزام اختيار عضوية واحدة ضمن الشركة المستهدفة. |

### 3.4 سلامة القيود والتكامل المالي

| المعرف | الخطورة | الدليل | الأثر المالي والبرمجي | الحل الأفضل |
|---|---:|---|---|---|
| F-01 | حرج | `FinancialIntegrationService.createAutomaticJournalEntry` ينشئ رأس قيد بحالة `posted` قبل إدخال البنود. | يمكن أن يظهر قيد مرحّل بلا بنود أو بلا توازن عند فشل الإدخال اللاحق؛ كما أن التطبيق يقوم بعمليات منفصلة بلا معاملة ذرية. | منع إدخال قيود مرحّلة مباشرة. تُرسل البنود إلى RPC خادمية واحدة تنشئ الرأس والبنود، تتحقق من التوازن، ثم ترحّل داخل معاملة PostgreSQL واحدة. |
| F-02 | حرج | خدمة التكامل المالي تعمل في المتصفح وتكتب مباشرة في `journal_entries` و`journal_entry_items` و`accounting_transactions`. | يمكن تجاوز قواعد العمل أو إنشاء قيود مكررة، وتصبح سلامة العملية معتمدة على اتصال العميل. | قصر إنشاء القيود على محرك ترحيل خلفي؛ الواجهة ترسل حدثاً تجارياً مع مفتاح idempotency فقط. |
| F-03 | حرج | `resolveAccountIds` يبحث عن رقم الحساب بلا `company_id`، ويستعمل عضوية أولى بـ`limit(1)`. | احتمال استعمال حساب شركة أخرى أو شركة غير افتراضية؛ وهذا خطر مباشر في نظام متعدد الشركات. | جعل `company_id` إلزامياً في كل طلب ترحيل، وتقييد الاستعلامات والدوال والحسابات بمفتاح الشركة. |
| F-04 | حرج | خدمة `accounting-automation` تنشئ قيوداً بمبالغ كلية مدينة أو دائنة حسب النوع، لا ببنود مزدوجة متوازنة. | قد ينشئ مساراً مالياً غير متوازن أو غير قابل للتقرير السليم، ويتعارض مع نموذج بنود القيود الحالي. | إيقاف هذا المسار وإعادة بنائه كمحوّل أحداث إلى محرك القيود الموحد؛ لا يقبل المحرك إلا مجموعة بنود متوازنة. |
| F-05 | مرتفع | مسار الرواتب موجود مرتين: `FinancialIntegrationService.linkEmployeeSalary` من الواجهة و`create_salary_journal_entry` من قاعدة البيانات. | احتمال تكرار قيد الراتب لنفس سجل الرواتب، واختلاف الحسابات والأرقام المرجعية بين المسارين. | اعتماد دالة واحدة للرواتب تستقبل `salary_record_id` وتفرض قيداً فريداً على مصدر العملية، مثل `unique(company_id, source_type, source_id, event_type)`. |
| F-06 | مرتفع | الترحيلات الحديثة تحمي القيد بعد الحالة `posted` وتتحقق من التوازن عند **تحديث** الحالة. | الضابط جيد، لكنه لا يكفي إذا أُدخل رأس جديد مرفق بحالة posted عبر مسار مختلف. | إضافة تحقق عند INSERT وUPDATE، ومنح عملية الترحيل للدالة الموثوقة وحدها؛ مع منع DML المباشر للمستخدمين على جداول الأستاذ. |
| F-07 | مرتفع | إشعار المعاملات الكبيرة ورسائل البريد تستخدم `finance@company.com` ثابتاً. | تنبيهات مالية قد تُرسل إلى جهة خاطئة أو لا تصل للمسؤولين الفعليين. | إعداد مستلمي التنبيهات لكل شركة، وتسجيل محاولات الإرسال وفشلها في outbox قابل للمراجعة. |
| F-08 | مرتفع | خدمة `DataIntegrationService` تنشئ عميلاً افتراضياً، وتربط به المشاريع غير المرتبطة، وتعيّن مهام عشوائياً للموظفين، وتحسب رأس المال بنسبة ثابتة 80/20. | هذا ليس تحقق سلامة؛ بل تعديل غير معتمد لبيانات تشغيلية ومالية، قد يغير التقارير والملكية من دون موافقة. | تعطيل هذه الدالة في الإنتاج. استبدالها بتقارير استثناءات وقائمة إصلاح يوافق عليها المستخدم، مع أوامر صريحة وسجل تدقيق. |
| F-09 | مرتفع | لا توجد طبقات معيارية مكتملة للمبيعات والمشتريات والمخزون والمدفوعات والتحصيل؛ كما لا توجد جداول أساسية بأسماء أو أدوار هذه الدورات. | لا يمكن تحقيق دورة مستندية كاملة أو فواتير إلكترونية أو تكلفة مبيعات أو تقييم مخزون موثوق. | بناء الوحدات التجارية الأساسية قبل اعتبار النظام ERP محاسبياً متكاملاً. |

### 3.5 التقارير والمؤشرات ولوحات المعلومات

| المعرف | الخطورة | الدليل | الأثر | الحل الأفضل |
|---|---:|---|---|---|
| R-01 | مرتفع | `FinancialMetricsService` يتعامل مع `account_id` كأنه رقم حساب يبدأ بـ`1` أو `4`، بينما الحقل معرف UUID. | معظم النسب قد تكون صفراً أو غير صحيحة حتى مع وجود قيود سليمة. | حساب النسب عبر SQL views أو RPC ترتبط بدليل الحسابات ونوع الحساب ورقمه، وليس عبر UUID أو وصف نصي. |
| R-02 | مرتفع | المؤشرات المالية تسترجع معاملات وقيوداً من دون فلتر واضح للشركة، وتخزن cache بمفتاح عام في المتصفح. | تداخل بيانات شركات متعددة أو ظهور أرقام قديمة بعد تغيير الشركة أو الترحيل. | تضمين `company_id` والفترة وإصدار بيانات الأستاذ في مفتاح الكاش، وإبطال الكاش عند ترحيل أو عكس قيد. |
| R-03 | متوسط | `accountingReports.ts` يعتمد على بنود القيود المرحّلة، وهو صحيح أساساً، لكنه لا يمرر الشركة صراحة. | يمكن جمع بيانات شركات متعددة للمستخدم ذي العضويات المتعددة. | جعل الشركة النشطة مدخلاً إجبارياً لجميع التقارير والاستعلامات وRLS. |
| R-04 | متوسط | قائمة المركز المالي تحسب صافي دخل الفترة ثم تضيفه إلى حقوق الملكية من حركات الفترة فقط. | يلزم ضبط فترات الافتتاح والإقفال والأرباح المرحلة؛ وإلا قد تظهر حقوق الملكية ناقصة أو مزدوجة. | إدخال فترات مالية، حسابات أرباح مرحلة وإقفال، وتقارير رصيد افتتاحي/حركة/رصيد ختامي. |
| R-05 | مرتفع | فواتير المشاريع تتكون من رقم ومبلغ وحالة وتواريخ فقط، ولا تشمل العميل أو البنود أو الضريبة أو العملة أو التحصيل أو مرجع القيد. | لا تمثل فاتورة مبيعات قابلة للتدقيق أو للفوترة الإلكترونية أو للترحيل الآلي. | استبدالها بنموذج فاتورة موحد: header/lines/taxes/settlements/e-invoice metadata/journal link. |

### 3.6 الواجهة وتجربة الاستخدام

| المعرف | الخطورة | الدليل | الأثر | الحل الأفضل |
|---|---:|---|---|---|
| U-01 | متوسط | واجهة `/auth` ظهرت سليمة بعد تأخر أولي؛ بداية التحميل كانت صفحة بيضاء قبل اكتمال العرض. | قد يظن المستخدم أن النظام تعطل على الشبكات البطيئة. | جعل شاشة تحميل التطبيق جزءاً من HTML الأولي أو fallback واضحاً، وقياس زمن أول عرض. |
| U-02 | مرتفع | التنقل يعرض لوحة تنفيذية وإدارة وفئات مالية لكل مستخدم، ولا يحتوي metadata للصلاحيات. | تضارب بين ما يراه المستخدم وما يستطيع فعله، وزيادة السطح المعرض للأخطاء. | إضافة `requiredPermission` إلى كل عنصر قائمة ومسار وإجراء، ثم توليد القائمة المسموح بها فقط. |
| U-03 | متوسط | `useRealtimeSync` يستعمل جدول `employee_salaries` الذي لا يظهر ضمن مخطط الجداول المولد، بينما يوجد `salary_records`. | اشتراك فوري غير فعّال أو رسائل تحديث لا تصل للمكونات الصحيحة. | اعتماد أسماء جداول مركزية مشتقة من الأنواع المولدة، ثم اختبار الاشتراكات في بيئة تكامل. |
| U-04 | مرتفع | بيانات المستخدم في التخطيط والتنقل ثابتة، ومحتوى الواجهة يعلن توافقاً تشغيلياً واسعاً بينما الوحدات الجوهرية غير مكتملة. | خطر تضليل المستخدمين وفرق العمل حول مستوى الجاهزية. | عرض اسم المستخدم والشركة والدور الفعلي، ووضع ميزات غير مكتملة خلف feature flags بدلاً من الإعلان عنها كمنجزة. |

## 4. مصدر التعارض الرئيسي

المشروع يحتوي على **نموذجين متنافسين** لتسيير المال. النموذج الأول، وهو الأنسب للاعتماد، يستخدم `journal_entries` و`journal_entry_items` ودليل الحسابات مع دوال وترحيلات تحمي القيد بعد ترحيله وتتحقق من توازنه. النموذج الثاني يعتمد خدمات واجهة قديمة وجداول معاملات عامة، ويكتب الرؤوس والبنود مباشرة أو ينشئ مبالغ إجمالية لا تمثل قيداً مزدوجاً كاملاً. لا يمكن تشغيل النموذجين معاً من دون ازدواج أو تعارض أو أرقام تقارير غير موثوقة.

> **القرار المعماري المطلوب:** اعتماد دفتر الأستاذ ذي البنود المزدوجة باعتباره المصدر المالي الوحيد للحقيقة. لا ينشئ أي موديول تشغيلي أو مكوّن React قيداً بنفسه؛ بل يرسل حدثاً موثقاً إلى محرك ترحيل مركزي.

## 5. البنية المستهدفة لنظام مترابط

### 5.1 طبقات النظام

| الطبقة | المسؤولية | قاعدة لا تقبل الاستثناء |
|---|---|---|
| الهوية والكيانات | المستخدمون، العضويات، الشركات، الفروع، الشركة النشطة. | كل طلب يحمل شركة نشطة ومتحققاً منها خادمياً. |
| الصلاحيات | الدور والصلاحيات الدقيقة وفصل المهام والموافقات. | لا تعتمد الواجهة وحدها؛ القرار النهائي في RLS/RPC/Edge Function. |
| البيانات الرئيسية | العملاء، الموردون، الأصناف، المستودعات، الضرائب، دليل الحسابات، مراكز التكلفة. | كل سجل تابع لشركة، ويُنشأ ويُراجع قبل استخدامه في مستند مالي. |
| المستندات التشغيلية | عروض، أوامر، استلام، فواتير، إشعارات دائن/مدين، دفعات، رواتب. | لا تؤثر مالياً قبل الوصول إلى حالة عمل معتمدة. |
| محرك الترحيل | تحويل الحدث التشغيلي إلى بنود مدين/دائن متوازنة. | معاملة ذرية، مفتاح idempotency، وربط كامل بالمصدر. |
| دفتر الأستاذ والتقارير | القيود المرحّلة وبنودها والأستاذ وميزان المراجعة والقوائم. | مصدر التقارير هو البنود المرحّلة فقط، لا الجداول الوسيطة أو أوصاف نصية. |
| التدقيق والتشغيل | سجل مراجعة، صندوق رسائل، مراقبة الأخطاء، مهام خلفية. | لا تعديل صامت للبيانات؛ كل حدث قابل للتتبع والاسترجاع. |

### 5.2 نموذج الشركة النشطة والعزل متعدد الشركات

يجب أن تتضمن كل الجداول التجارية والمالية `company_id NOT NULL` مع مفتاح أجنبي مناسب وفهارس مركبة تتضمن الشركة. لا يكفي أن تكون للمستخدم عضوية في أكثر من شركة؛ يجب أن يحدد كل طلب شركة نشطة، ثم تتحقق الدالة الخادمة من أن المستخدم عضو فيها ويملك الصلاحية المطلوبة عليها.

ينبغي أن يصبح تحقق الصلاحية بالشكل المفاهيمي التالي: **المستخدم + الشركة + الإجراء + الكيان**. على سبيل المثال، لا يكفي أن يحمل الشخص دور `accountant` في أي شركة؛ يجب أن تكون له صلاحية `post_journal_entries` في الشركة التي يتبع لها القيد المطلوب ترحيله.

### 5.3 محرك القيود الموحد

يتلقى المحرك حدثاً مثل `sales_invoice_issued` أو `supplier_invoice_approved` أو `payroll_accrued` أو `bank_payment_posted`. ويتحقق من الشركة، الفترة المالية، حالة المستند، الصلاحية، وتكرار الحدث، ثم ينشئ رأس قيد في حالة draft وبندين أو أكثر. بعد التأكد من أن مجموع المدين يساوي مجموع الدائن، يغير الحالة إلى posted داخل المعاملة نفسها، ويسجل رابط المصدر وسجل التدقيق.

| نوع الحدث | المستند المصدر | الأثر المالي النموذجي | متطلب عدم التكرار |
|---|---|---|---|
| إصدار فاتورة مبيعات | Sales Invoice | مدين: ذمم العملاء؛ دائن: إيراد وضريبة مخرجات عند الاقتضاء. | قيد فريد لكل `invoice_id` وحدث إصدار. |
| تسليم مخزون لبيع | Shipment / Delivery | مدين: تكلفة المبيعات؛ دائن: المخزون. | قيد فريد لكل حركة مخزون مرحّلة. |
| اعتماد فاتورة مورد | Supplier Invoice | مدين: مخزون أو مصروف أو أصل وضريبة مدخلات؛ دائن: ذمم الموردين. | قيد فريد لكل `supplier_invoice_id`. |
| دفع راتب أو استحقاقه | Payroll Run | مدين: مصروفات رواتب؛ دائن: رواتب مستحقة واستقطاعات. | قيد فريد لكل دورة رواتب/موظف وفق سياسة التجميع. |
| تحصيل أو دفع بنكي | Bank Transaction | تأثير النقدية مقابل الذمم أو المصروف أو الالتزام. | قيد فريد لكل حركة بنك مستوردة أو مؤكدة. |
| عكس أو تصحيح | Reversal Request | قيد عكسي كامل؛ لا تعديل للقيد الأصلي المرحّل. | رابط صريح بالقيد الأصلي ومفتاح فريد للعكس. |

### 5.4 استخدام المعاملة الذرية ومفتاح التكرار

ينبغي أن تكون العملية التي تنشئ القيد والبنود وحركة الربط وسجل التدقيق ضمن دالة قاعدة بيانات واحدة أو خدمة خلفية واحدة تستخدم معاملة قاعدة بيانات. هذا يمنع نصف القيد، ويضمن أن فشل أي خطوة يؤدي إلى التراجع الكامل.

يُضاف إلى القيد جدول أو حقول أصل مثل `source_type` و`source_id` و`accounting_event` و`idempotency_key`. ويُفرض قيد uniqueness مركب يمنع إنشاء قيدين عند إعادة الطلب بسبب إعادة المحاولة أو انقطاع الشبكة.

### 5.5 عدم قابلية التعديل والتصحيح

تظل القيود المرحّلة للقراءة فقط. ويجب أن تمنع قاعدة البيانات تحديث رأس القيد أو بنوده بعد الحالة `posted`، بما في ذلك الإدخال اللاحق للبنود. ويُعالج الخطأ بقيد عكسي أو قيد تصحيحي موثق يتضمن سبب التصحيح ومرجع القيد الأصلي والمستخدم المعتمد.

## 6. الوحدات المطلوبة لاستكمال ERP الحقيقي

النظام الحالي أقرب إلى منصة إدارة داخلية تجمع الموارد البشرية والمشاريع والحسابات الأولية. لكي يصبح ERP مترابطاً، يجب إنشاء الوحدات التالية بالترتيب؛ لا يوصى ببدء الفوترة الإلكترونية قبل اكتمال المستند المالي والدورة المحاسبية الأساسية.

| الأولوية | الوحدة | الجداول/الكيانات الأساسية | التكامل الإلزامي |
|---:|---|---|---|
| 1 | الأساس المالي | فترات مالية، دليل حسابات، مراكز تكلفة، قيود، بنود قيود، عكس، موافقات. | جميع الوحدات اللاحقة. |
| 2 | العملاء والتحصيل | عملاء، عناوين، شروط دفع، فواتير مبيعات، بنود، ضرائب، تحصيلات، تسويات. | الذمم، الإيراد، الضريبة، البنك. |
| 3 | الموردون والمشتريات | موردون، طلبات، أوامر، استلامات، فواتير موردين، دفعات. | الذمم، المخزون/المصروف/الأصل، الضريبة، البنك. |
| 4 | المخزون | أصناف، وحدات قياس، مستودعات، حركات مخزون، طبقات تكلفة، جرد. | FIFO أو المتوسط المرجح، تكلفة مبيعات، قيود مخزون. |
| 5 | البنوك | حسابات بنكية، استيراد كشف، حركات، مطابقة، تسويات. | النقدية، الذمم، قيود المطابقة. |
| 6 | الرواتب | دورات رواتب، عناصر، استقطاعات، اعتمادات، دفعات، قيود. | المصروفات والالتزامات والبنك ومراكز التكلفة. |
| 7 | الفوترة الإلكترونية | بيانات الفاتورة، الضرائب، QR، التوقيع/الإرسال وحالات الاستجابة. | فاتورة مبيعات معتمدة وسجل تدقيق. |

## 7. خطة التنفيذ المقترحة

### المرحلة صفر: إيقاف المخاطر ومنع تلوث البيانات

في هذه المرحلة لا تُضاف ميزات جديدة. يُعطل تشغيل `DataIntegrationService.ensureDataIntegrity` في بيئة الإنتاج، ويُوقف إنشاء القيود من خدمات المتصفح أو الدالة القديمة غير المتوازنة. كما تُزال القيم الثابتة الخاصة بالمستخدمين والبريد المالي، وتُخفى الوحدات غير المكتملة خلف feature flags.

**معيار النجاح:** لا يستطيع أي مستخدم مصادق عليه إنشاء أو تعديل أو ترحيل قيد من استدعاء مباشر لجداول الأستاذ، ولا تقوم أي أتمتة بإنشاء عميل افتراضي أو ربط مشروع أو تعيين موظف عشوائياً.

### المرحلة الأولى: توحيد الهوية وRBAC والشركة النشطة

يُستبدل نموذج الأدوار المختلط بمرجع موحد. تُصحح الأدوار القديمة في البيانات والواجهات والدوال، ثم تُنشأ طبقة سياق للجلسة تحتوي على المستخدم والشركة النشطة والدور والصلاحيات. وتتحول حماية المسارات والقائمة والأزرار إلى استخدام الصلاحيات الدقيقة، بينما تبقى قاعدة البيانات والوظائف الخادمة جهة المنع النهائية.

**معيار النجاح:** يستطيع المستخدم متعدد الشركات التبديل بين الشركات المصرح بها، ولا يرى أو يوافق أو يرحّل بيانات شركة أخرى، ويظهر اسمه ودوره وشركته الحقيقية في الواجهة.

### المرحلة الثانية: محرك ترحيل موحد ودفتر أستاذ موثوق

يُنفذ RPC أو Edge Function موثوق لإنشاء القيود من أحداث موثقة فقط. تُنقل الرواتب ورأس المال والمصاريف الحالية إلى هذا المحرك، ثم تُلغى الخدمات الموازية. ويُستكمل فرض عدم التوازن على INSERT وUPDATE، مع قيد uniqueness لمصدر العملية، وإدارة فترات الإقفال والعكس.

**معيار النجاح:** كل قيد مرحّل يحتوي بندين أو أكثر ومتوازن، ويمكن الوصول منه إلى مستنده المصدر، ولا يمكن تكراره عند إعادة الإرسال أو تعديله بعد الترحيل.

### المرحلة الثالثة: دورة المبيعات والمشتريات والمخزون

تُبنى الجداول والواجهات وحالات العمل والموافقات من البداية على نموذج المستند المصدر. ويصبح إصدار الفاتورة أو استلام البضاعة أو دفع المستحقات أحداثاً تؤدي إلى محرك القيود، لا إلى تحديثات واجهة متفرقة. تُضاف الفوترة الإلكترونية فقط بعد استقرار نموذج فاتورة المبيعات والضرائب.

**معيار النجاح:** يمكن تتبع فاتورة العميل من البنود والضريبة والتحصيل إلى القيد والأستاذ والتقرير، ويمكن تتبع فاتورة المورد أو حركة المخزون بالطريقة نفسها.

### المرحلة الرابعة: التقارير والأداء والجودة التشغيلية

تُنقل حسابات المؤشرات إلى views أو RPC محاسبية مبنية على بنود القيود والدليل والفترة والشركة. تُضاف اختبارات ثابتة للتوازن والعكس وعدم التكرار وعزل الشركة، ثم تقسيم للواجهة وحزمها، ومراقبة أخطاء وإشعارات قابلة للضبط لكل شركة.

**معيار النجاح:** تتطابق ميزانية المراجعة مع دفتر الأستاذ والقوائم، وتعمل لوحات المؤشرات على الشركة والفترة المحددتين، ويمر البناء والاختبارات وفحص الجودة في CI.

## 8. خطة الاختبارات والقبول

| الاختبار | السيناريو | النتيجة المقبولة |
|---|---|---|
| توازن القيد | محاولة ترحيل بنود مجموع مدينها لا يساوي مجموع دائنها. | رفض كامل للعملية، بلا رأس قيد أو بنود جزئية. |
| عدم التكرار | إرسال طلب ترحيل فاتورة أو راتب بالمفتاح نفسه مرتين. | إنشاء قيد واحد فقط وإرجاع النتيجة الأصلية. |
| العزل متعدد الشركات | مستخدم بدور محاسب في شركة أ يحاول قراءة أو اعتماد قيد في شركة ب. | رفض الوصول على مستوى RLS وRPC والواجهة. |
| عدم التعديل بعد الترحيل | محاولة تعديل بند أو حذف قيد posted. | رفض قاعدة البيانات، مع السماح بطلب عكس موثق فقط. |
| اعتماد القيد | منشئ القيد يحاول اعتماده عندما تقتضي السياسة فصل المهام. | رفض أو إحالة لمستخدم مفوض مختلف حسب سياسة الشركة. |
| دورة الرواتب | إنشاء دورة رواتب ثم إعادة المحاولة بعد انقطاع. | قيد واحد متوازن، مرتبط بالدورة والموظف/الدفعة. |
| فاتورة مبيعات | إصدار فاتورة ثم تحصيل جزئي ثم إشعار دائن. | تتطابق الذمم والإيراد والضريبة والتحصيل والأرصدة النهائية. |
| المطابقة البنكية | مطابقة حركة كشف بحركة دفتر ثم إلغاء المطابقة. | قيد تسوية موثق أو عكسه، بلا تغيير صامت للحركة الأصلية. |
| التقارير | مقارنة ميزان المراجعة ودفتر الأستاذ والقائمة المالية لنفس الشركة والفترة. | مجموع المدين = مجموع الدائن، واتساق الأرصدة والقوائم. |
| الاسترداد والمصادقة | رابط استعادة صحيح ومنتهي، ثم تغيير كلمة المرور. | تدفق واضح وآمن مع معالجة الحدث والجلسة ورسائل عربية دقيقة. |

## 9. القرارات التي يجب اعتمادها قبل البدء بالتطوير

| القرار | الخيارات | التوصية |
|---|---|---|
| نطاق الكيانات | شركة واحدة حالياً أو عدة شركات/فروع منذ البداية. | تصميم متعدد الشركات منذ البداية لأن المخطط الحالي يتضمن عضويات شركات؛ اجعل الشركة النشطة إلزامية. |
| نموذج الصلاحيات | أدوار ثابتة فقط أو أدوار مع صلاحيات دقيقة. | أدوار افتراضية مع permissions دقيقة قابلة للضبط، وفصل صلاحيات الإنشاء والاعتماد والترحيل. |
| مسار الترحيل | من الواجهة مباشرة أو محرك خلفي موحد. | محرك خلفي موحد حصراً. |
| سياسة الرواتب | قيد لكل موظف أو قيد تجميعي للدورة مع تفصيل مساعد. | قيد تجميعي للدورة وفق مراكز التكلفة مع روابط تفصيلية للموظفين، ما لم تقتض سياسة التقارير غير ذلك. |
| تقييم المخزون | FIFO أو المتوسط المرجح. | اختيار واحد لكل صنف/سياسة قبل ترحيل أول حركة؛ لا تغيره من دون إجراء محاسبي موثق. |
| اعتماد القيود | اعتماد ذاتي أو فصل مهام. | فصل المهام: المنشئ لا يعتمد ولا يرحّل ما أنشأه، مع استثناءات موثقة للمنشآت الصغيرة. |
| مسار الإصلاح | إصلاح تدريجي في المستودع الحالي أو إعادة بناء وحدات مالية معزولة. | إصلاح تدريجي منظم: الاحتفاظ بالواجهة والنواة، وإعادة بناء الطبقة المالية والصلاحيات على حدود واضحة. |

## 10. الملفات الرئيسية التي استند إليها التحليل

| المجال | ملفات أو ترحيلات تمت مراجعتها |
|---|---|
| المسارات والمصادقة | `src/App.tsx`، `src/hooks/useAuth.ts`، `src/components/auth/ProtectedRoute.tsx`، `src/pages/auth/Login.tsx`، `src/pages/auth/ResetPassword.tsx` |
| الأدوار وإدارة المستخدمين | `src/types/permissions.ts`، `src/hooks/usePermissions.ts`، `src/components/admin/UserManagementPanel.tsx`، `src/components/admin/UserRoleForm.tsx`، `supabase/functions/admin-users/index.ts` |
| الشركات وRLS | `supabase/migrations/20260106145801_ea6d1c06-f44b-4a32-ad6c-64b1817a7f86.sql`، `supabase/migrations/20260107090851_8e783997-8a8e-4c16-89ce-2a22853d3d06.sql` |
| دفتر الأستاذ والاعتماد | `supabase/migrations/20260802035627_99126db3-dc14-4d7d-93f5-c2683ffe639d.sql`، `supabase/migrations/20260802040005_ff60a546-cb96-4908-8b29-8717a9c578dc.sql`، `supabase/migrations/20260802035147_09246d2d-50d9-41f7-aaf3-2d6bbcd30952.sql` |
| الخدمات المالية | `src/services/financialIntegrationService.ts`، `src/services/automationService.ts`، `src/services/employeeAccountingService.ts`، `supabase/functions/accounting-automation/index.ts` |
| التقارير واللوحات | `src/services/accountingReports.ts`، `src/services/financialMetrics.ts`، `src/pages/dashboard/Index.tsx` |
| الواجهة والتنقل | `src/components/AppLayout.tsx`، `src/components/AppNavigation.tsx`، `src/data/navigationMenu.tsx`، `src/hooks/useRealtimeSync.ts` |
| تكامل البيانات وفواتير المشاريع | `src/services/dataIntegrationService.ts`، `src/components/projects/details/ProjectInvoices.tsx` |

---

## التوصية النهائية

لا أوصي بإضافة موديولات جديدة أو توصيل فواتير إلكترونية على الوضع الحالي قبل تنفيذ **المرحلتين صفر والأولى والثانية**. بعد ذلك يمكن بناء المبيعات والمشتريات والمخزون فوق قاعدة موثوقة. هذا المسار يحافظ على الواجهة والاستثمار القائم، لكنه يمنع تكرار القيود وتداخل الشركات وتضارب الصلاحيات، ويحوّل النظام من مجموعة صفحات وخدمات متفرقة إلى ERP مترابط يمكن تدقيقه مالياً وبرمجياً.
